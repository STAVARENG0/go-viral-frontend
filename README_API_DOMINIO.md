# Atualização da API

Frontend configurado para usar:

https://api.matheus-caetano.com

Arquivo principal:
- `.env.production` com `VITE_API_BASE_URL=https://api.matheus-caetano.com`
- `src/main.jsx` com fallback atualizado

Depois de subir no GitHub, o GitHub Actions vai gerar o build automaticamente.
